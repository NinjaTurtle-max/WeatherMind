# 환경변수 · 시크릿
.env
# `.env`만 적으면 **정확히 그 이름만** 무시된다 — `.env.bak-…`처럼 사람이 손으로
# 뜨는 백업은 그대로 추적 대상이 되고, 그 안에 DB 암호·JWT 시크릿이 들어 있다
# (규정상 키 노출 = 실격). `.env.example`은 이미 추적 중이라 영향받지 않는다.
.env.bak*
.env.local
*.pem

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Node
node_modules/
frontend/dist/

# OS / 에디터
.DS_Store
.idea/
.vscode/

# 데이터 볼륨
chroma-data/
postgres-data/

# Redis 스냅샷 — redis-server를 로컬에서 띄우면 실행 디렉터리에 떨군다
# (compose 밖에서 스택을 돌릴 때 backend/ 아래 생겼다). 런타임 산출물이라 커밋 금지.
dump.rdb

# DB 수동 백업 산출물 (scripts/db_backup.sh once — 유저 데이터 포함, 커밋 금지)
backups/

# Claude 프롬프트 기록 (개인 로그)
.claude/prompt-logs/

# 병렬 작업용 git 워크트리 (TEAM_PROCESS §2.6.1) — 저장소에 커밋하지 않는다
.claude/worktrees/

# 해커톤 제출 서류 스캔 — 팀원 서명·개인정보 포함, 커밋 금지 (MENTORING_ALIGNMENT §7)
docs/SCAN*.PDF
docs/SCAN*.pdf

# 대회 주최측 배포 자료 — 저작권이 우리 것이 아니고 제출물에도 들어가면 안 된다.
# (2026-08-07 감사: RAG 철거 커밋에 목적과 무관하게 혼입돼 있었다)
ai_OT자료/
