# WeatherMind에 기여하기 — 5가지 규칙

팀원용 최소 규칙입니다. 상세 절차는 `docs/team/TEAM_PROCESS.md` §2.4에 있지만,
아래 5개만 지키면 충돌 없이 함께 작업할 수 있습니다.

## 1. main에 직접 올리지 않기 (웹 업로드 포함)

GitHub 웹의 **"Add files via upload"도 main 직접 커밋입니다.** 진행 중인 작업과
경합하면 업로드 화면에서 충돌·오류가 반복해서 뜹니다 — 실제로 겪으셨다면 이것이
원인입니다.

**대신 이렇게:**
- 웹에서 올릴 때: 업로드 화면 하단에서 **"Create a new branch and start a pull
  request"** 를 선택 (main 직접 커밋 대신 — 클릭 한 번 차이입니다)
- 또는 이미지·문서 같은 에셋은 **저에게(팀 채널로) 전달**해 주시면 진행 중인
  작업에 편입합니다

## 2. 브랜치 이름은 `<type>/<주제>`

예: `feat/mascot-images`, `fix/login-typo`, `docs/readme`.
type은 feat(기능)·fix(수정)·docs(문서)·chore(정리) 중 하나면 충분합니다.

## 3. PR을 열면 CI가 자동으로 검사합니다

lint·테스트·프론트 스모크가 자동으로 돕니다. **빨간 ✗가 뜨면 병합하지 않고**
결과 링크를 확인하거나 저에게 알려 주세요. 초록 ✓ 후 **"Merge pull request"**
(기본 병합 — Squash 아님)로 병합합니다.

## 4. 히스토리를 다시 쓰는 명령 금지

`push --force` · `rebase` · `reset --hard` · `filter-*`는 쓰지 않습니다.
이 저장소는 과거에 히스토리 재작성으로 **로컬 파일까지 영구 소실**된 사고가
있었습니다(CLAUDE.md 기록). 뭔가 꼬였다 싶으면 지우지 말고 그대로 두고 문의해
주세요 — 커밋된 것은 거의 항상 복구할 수 있지만, 지운 것은 아닙니다.

## 5. 큰 파일·비밀값은 커밋 전에 한 번 확인

- 이미지가 1MB를 넘으면 압축 후 업로드(기존 마스코트 PNG는 ~100KB 수준)
- `.env`·API 키·비밀번호는 절대 커밋하지 않기 (`.env.example`에 자리만)

---

질문은 언제든: 이 저장소의 개발 진행·병합은 PM(Claude 세션)이 맡고 있으므로,
무엇이든 애매하면 **일단 올리지 말고** 물어봐 주시는 쪽이 빠릅니다.
