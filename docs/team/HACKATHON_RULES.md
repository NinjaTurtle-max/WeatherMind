# 대회 규정·제출 요건 — 공식 가이드 실사 정리

**출처: `https://guide.weatherhackathon.kr` 전 8트랙 직접 열람(2026-08-06, PM).**
주최 기상청 · 주관 기상기후인재개발원 · 후원 기후에너지환경부.

이 문서는 **대회 규정의 SSOT**다. 개발 계획(ROADMAP)과 충돌하면 **이 문서가 이긴다** —
규정은 우리가 협상할 수 없는 유일한 제약이다.

> 읽는 법: §1~§3이 규정 원문 요약, **§4가 우리 계획과의 충돌 12건**, §5가 실행 항목,
> §6이 운영팀에 물어야 할 모호 지점이다. 시간이 없으면 §4·§5만 봐도 된다.

---

## 1. 제출 산출물 4종 (트랙 05)

**"어떤 환경에서 제작했는지 관계없이"** 아래 4개만 충족하면 된다 — 웹 Claude Code를
쓰든 터미널·IDE를 쓰든 무방하다고 홈·FAQ·트랙 01이 세 번 반복해 명시한다.
**우리의 CLI 기반 멀티서비스 개발은 규정 위반이 아니다.**

| # | 산출물 | 요건 원문 요지 | 우리 상태 |
|---|---|---|---|
| ① | **구동 가능한 URL** | 누구나 접속해 **바로 조작**. **로그인·결제 없이 열려야** 함. Vercel·GitHub Pages·Netlify **등 어떤 배포 방식이든 인정**. 크롬 기준, **1366×768·1920×1080·2560×1440** 지원 | ❌ 서버 미확보(8/10 기한) · ⚠️ 첫 화면이 로그인 페이지(§4-A) |
| ② | **소스 코드 zip** | **구글 폼 업로드**(운영팀 별도 안내). **GitHub 링크 제출 아님**. `node_modules`·빌드 산출물 제외, **소스만** | ⚠️ 미준비 — 제외 규칙 정의 필요(§4-D) |
| ③ | **주요 프롬프트 세션** | AI 활용 근거가 되는 주요 대화 세션을 **`.md` 또는 `.txt`로 내보내기 → zip → 구글 폼** | 🟢 원재료 자동 확보(11세션·13,482줄·7/07~8/06 전 구간) — 변환·선별·재스캔만(§4-B) |
| ④ | **구동 환경/배포 문서 (README)** | **필수.** 대회 이후 공공 교육용 실행·배포가 가능하도록 꼼꼼히. **외부 리소스(라이브러리·이미지·폰트·데이터) 출처·라이선스 명시** | ⚠️ README 96줄 존재하나 라이선스 절 없음(§4-C) |

추가로 **"실행에 필요한 모델과 데이터셋을 구글폼으로 제출합니다. (필수)"** —
우리는 LLM 가중치를 배포하지 않으므로 해당하는 것은 **시드 데이터셋**
(`database/seed/*.json`, 문항 뱅크)과 **IRT/BKT 파라미터**다. 소스 zip에 포함되지만
"모델·데이터셋" 항목으로도 명시적으로 짚어 README에 위치를 적는다.

### 1.1 제출 유의사항 (원문 3줄 — 전부 일정 제약)

1. **URL은 시상식 일정(9월 셋째 주 예상)까지 유지**되어야 한다.
2. **8/21 18:00 이후 소스코드 변경 불가**(논의 후 변경 가능). 심사는 **데모 테스트
   화면 녹화**로 진행 예정.
3. **검증 기간 중 실행 시 핵심 로직 수정 또는 소스코드 누락이 확인되면 탈락.**

### 1.2 ⚠️ OT 슬라이드는 산출물을 **5종**으로 적는다 — 가이드 4종과 다르다

출처: `ai_OT자료/기상기후 AI 해커톤 사전 교육 오리엔테이션_260722.pdf` **원문 직접
확인(2026-08-08)**. FAQ(본 대회) 원문:

> *"8.21.(금) 18시까지 제출 요건에 맞추어 구글폼으로 제출해 주셔야 합니다.
> 1) 발표자료(PPT or PDF) 2) 구동 가능한 URL 3) 소스 코드 4) 주요 프롬프트 세션
> 5) 구동 환경/ 배포 방법 문서(READ ME 등)"*

가이드 트랙 05와 사전교육 Mo2 체크리스트(17/18쪽)는 **4종**만 적는데 **OT FAQ는 맨 앞에
「발표자료(PPT or PDF)」를 더해 5종**이다. 위 §1 표의 ①~④에 **⓪ 발표자료**가 선행한다.

| # | 산출물 | 우리 상태 |
|---|---|---|
| ⓪ | **발표자료 (PPT or PDF)** — 8/21 18:00 구글폼 제출, 8/22 현장 발표에 그대로 사용 | ❌ 미착수. 담당·기한 미배정 (§5-F 신설) |

**두 번째 수정**: §1.1-2가 *"심사는 데모 테스트 화면 녹화로 진행 예정"*이라 적었는데
**OT 원문은 녹화가 아니라 현장 발표·시연**이다(§1.3-② 참조). 그 줄은 가이드 스냅샷
시점의 기술이고, OT 슬라이드가 더 구체적·후행이므로 **현장 시연이 맞다**.

### 1.3 일정·심사 규정 (OT 슬라이드 원문 — 4건, 2026-08-08 확인)

**① 중간점검 멘토링 — 8/10(월)~8/12(수) · 본선 15팀 전원 의무**

| 항목 | 원문 |
|---|---|
| 기간 | **8월 10일(월) ~ 8월 12일(수)**, 3일 |
| 방식 | **비대면 ZOOM** (ZOOM 링크·별도 발표용 슬라이드 배포 예정) |
| 대상 | 본선 진출 **15팀 전원 (의무 참여)** |
| 시간 | 일별 **13:00~18:00**, **팀당 50분** — 5팀/일 × 3일 = 15팀 |
| 구성 | 진행 상황·**프로토타입 공유 20~25분** + **멘토 4인** 질의·피드백·방향 논의 25~30분 |
| 순서 | 팀별 희망 일정 고려해 결정 |
| 반영 | **중간점검 결과는 최종 점수에 반영되지 않는다** |

> ⚠️ **원문 내부 불일치 1건**: 슬라이드는 "팀당 50분"·시간표도 13:00~13:50 식 50분
> 슬롯(사이 10분 휴식)인데, FAQ 문면은 *"팀 별 한 시간씩"*이라 적는다. **50분이
> 발표·논의 실시간, 60분은 휴식 포함 슬롯 간격**으로 읽는 것이 시간표와 정합한다.

> 🔴 **이미 지난 마감 1건 — 원문에만 있고 우리 문서 어디에도 없었다.**
> FAQ(사전개발): *"중간 점검을 위한 자료를 **8. 7.(금) 18시까지 제출**해야 합니다
> (발표 양식 별도 배포 예정)"*. 오늘(8/08) 기준 **경과**했다. 운영팀 공지·이메일에서
> 실제 배포된 발표 양식과 제출 여부를 **최우선으로 확인**할 것(§5-F-1).

**② 심사 = 8/22(토) 현장 발표 — 녹화가 아니다**

2일차 시간표 원문: 팀당 **20분** 슬롯 = **발표 10분 + 질의응답 5분 + 장내정리 5분**,
09:00~15:30에 3팀씩 5블록(15팀), 15:30~16:00 심사위원 채점, 16:00 폐회식.
**대회장 전원 배석**이고 **타 팀 발표에 대한 질의는 심사위원만** 가능하다.

- **발표·시연은 주최측이 준비한 PC로만** 한다 — **개인 PC 사용 불가**. 대회 1일차
  **19:00~21:00에 팀별 제출자료 이상 여부를 점검**한다. → 우리 데모가 **처음 보는
  PC의 크롬에서 URL만으로** 완주해야 한다는 뜻이다(§4-A 무인증 랜딩·§5 A-3 해상도
  QA가 여기서 두 번째 이유를 얻는다. 로컬 도커·개인 확장·저장된 로그인은 못 쓴다).
- 우수작은 당일 발표하지 않고 **9월 2주 기상청 누리집 공고**, **9월 3주 시상식**.
  (§4-E의 "URL 9월 셋째 주까지 유지"와 정합.)

> ⚠️ **원문 내부 불일치 2건**: FAQ는 *"시간은 팀당 15분입니다"*라 적으면서 곧바로
> *"(발표 10분/질의응답 5분/장내 정리 5분)"* = 20분을 나열한다. **2일차 시간표(팀당
> 20분 슬롯)가 운영 문서이므로 20분을 따른다.** 우리 준비 기준은 어느 쪽이든
> **발표 10분·질의 5분**으로 동일하다.

**③ 발표 시간 초과 감점 — 배점표 각주 원문**

> *"감점요소 : 결과발표 시 기준시간(10분)을 초과하여 발표 시 감점 발생
> (1분 초과 **-9점** 감점 / 2분 초과 **-15점** 감점 / 3분 초과 **-21점** 감점·발표 중지)"*

**정정**: 사내 감사 메모는 *"1분 초과가 ③학습효과 전체 10점보다 크다"*고 적었으나
**원문 수치로는 −9 < 10이라 틀리다.** 정확한 서술은 아래다.

| 초과 | 감점 | 배점표 대비 |
|---|---|---|
| 1분 | **−9** | ④ 아이디어 독창성 후단(7점)+α. ③ 학습효과 전체(10)의 **90%** |
| 2분 | **−15** | ③ 학습효과 전체(10)를 **넘는다.** ② 체험형 설계의 15점 항목 통째 |
| 3분 | **−21** | ① 주제 적합성 전체(20)를 **넘는다** + **발표 중지** |

즉 **"1분 초과가 한 항목보다 크다"는 2분 초과부터 참**이고, 1분 초과도 ④(15) 전체의
60%라 여전히 우리가 얻으려는 어떤 개선보다도 크다. **리허설로 9분 30초 안에 끝낸다.**

**배점표 원문 전문**(가이드 요약본이 아니라 OT 슬라이드 실물):

| 항목 | 평가 포인트 | 배점 |
|---|---|---|
| **① 주제 적합성 (20)** | 개발계획서에 제시한 기상·기후 개념이 다루어졌는가 | 10 |
| | 사용자가 결과물을 체험하며 그 주제를 명확하게 인식할 수 있는가 | 10 |
| **② 체험·참여형 설계 (25)** | 사용자의 조작·선택이 실제로 작동하고 학습 개념과 연계되는가 | 15 |
| | 단순 퀴즈·정답 맞히기를 **넘어** 변수를 바꿔보며 학습 탐구가 가능하고, 조작·선택을 직관적으로 파악하여 학습 개념을 해석할 수 있는가 | 10 |
| **③ 학습 효과 (10)** | 학습 대상이 명확하고 콘텐츠가 대상에 적합한 난이도인가 | 5 |
| | 학습 성과가 구체적이며, **잘못된 조작·선택에 대한 학습적 피드백**이 있는가 | 5 |
| **④ 아이디어 독창성 (15)** | 개발물의 독창성을 차별화 포인트를 들어 구체적으로 구현되었는가 | 8 |
| | 개발물이 사용자에게 새로운 학습 경험과 인터랙션을 제공하는가 | 7 |
| **⑤ 구현 완성도 (30)** | **URL 접속으로 핵심 기능·화면이 안정적으로 구동**되고, 계획한 핵심 기능과 체험이 실제로 작동하며 완성도가 높은가 | 15 |
| | 기상·기후 데이터가 개발물에 **실제 반영·작동**하고, **생성형 AI 도구가 결과물 제작 과정에서 효과적으로 활용**되었는가 | 15 |
| | **합계** | **100** |

(사전교육 Mo2 16/18쪽은 ⑤를 "실현 가능성 (30)"이라 부른다 — 같은 항목의 다른 표기다.
**배점·문면은 위 OT 배점표가 원본**이다.)

**④ 최종 제출 시 개인 계정 API 키 교체는 «기상청 API» 규정이다**

FAQ(Tool) 원문:

> *"Q. 기상청 API 사용 규정이 따로 있나요?
> A. 메일로 배포 예정인 **기상청 API(대용량)**는 개발·테스트하기 위해 대회 기간에만
> 제공됩니다. 단, **최종 소스 코드 제출 시점에는 반드시 본인 개인 계정으로 별도
> 발급받은 API key로 교체**해야 합니다."*

**이것이 §3 표(대회 제공 키 vs 팀 개인 발급 키)를 원문으로 확증한다.** §6-Q1이 물으려던
"두 키를 분리 운용하는 것이 맞는가"는 **이미 답을 받은 상태**이므로 질문에서 내린다.

> ⚠️ **범위 정정**: 감사 메모는 이 조항을 "API 키 일반"으로 일반화했으나 **원문 문맥은
> 기상청 대용량 API 한정**이다. Claude 계정은 별도 FAQ(Tool)에 있다 — *"7월 23일 ~
> 8월 22일까지 사용 가능"*, 팀플랜으로 **팀별 프리미엄 1인 + 그 외 스탠다드**.
> 규정상 "개인 키 교체 의무"가 명문화된 것은 **KMA 키뿐**이지만, §3의 "노출 = 실격"은
> *"외부 서비스에서 발급받은 어떤 키라도"* 동일하므로 **실무는 전 키 동일 취급**한다.

**부수 확인 3건**(원문에만 있고 우리 문서에 없던 것):

- **제출 후 수정 범위** — *"제출 이후 주요 기능의 수정은 불가능하고, 폰트 색상·크기·
  종류 등 마이너한 부분만 가능합니다. 주요 기능을 수정하거나 새로운 기능을 구현하였을
  경우 불이익이 발생할 수 있습니다."* → §4-G의 "8/21 18:00 전면 동결" 판단이 옳았음을
  원문이 확증한다.
- **발표 순서는 대회 1일차 추첨**으로 정해진다 — 미리 알 수 없다.
- **AI 생성 코드·이미지·음악 사용 전면 허용**, **기상청 마스코트 「기상이」 사용 가능**
  (단 §2 각주대로 저작권은 기상청). 비상업용 이미지·아이콘·폰트 사용 권장, 저작권
  있는 이미지는 출처 명확히 표기.

---

## 2. 규정 — 저작권 (트랙 04)

**"표절 및 저작권 검증을 실시합니다."** 팀 간·외부 출처 대비 표절 검증 +
**"제공 계정의 개발 세션 확인"**을 거친다. AI 사용 자체는 문제없으나 **다른 팀이나
외부 출처의 코드를 그대로 사용하는 것은 불가**. AI로 생성했어도 저작권·특허·보안
책임은 면제되지 않고 **최종 책임은 참가자**에게 있다.

| 항목 | 허용 ✓ | 주의 ⚠ |
|---|---|---|
| AI 생성 코드 | Claude Code 등으로 생성한 코드 사용 가능 | 다른 팀과 **동일한 코드 그대로** 제출 금지 |
| 오픈소스 | **MIT·Apache 2.0 등 상업적 허용** | **GPL 계열은 활용 범위 제약 가능. README 명시 필요** |
| 이미지·아이콘 | CC0·Unsplash·**직접 제작** | 출처 불명 이미지·유료 스톡 무단 사용 금지 |
| 폰트 | Google Fonts·Noto Sans 등 무료 폰트 | 상업용 폰트 무단 사용 금지. **라이선스 README 명시** |
| 데이터 | 출처·활용 가능 범위 확인 | **원출처 표기 필수** |

**기상청 자료**: 공공누리(KOGL) **유형을 확인하고 출처를 구체적으로 표시**. 제1유형
(출처표시)은 출처만 밝히면 자유이용, **제2·3·4유형은 상업적 이용·변형 등 제한** 확인
필요. FAQ 보충: 기상자료개방포털 데이터는 활용 가능하되 **개별 항목의 적용 가능성은
팀이 직접 확인**해야 하고, **대용량 API는 필수가 아니며** 공공데이터포털·국가기상
위성센터 등 다른 공공 데이터도 함께 쓸 수 있다.

**마스코트 주의**: 가이드 각주에 *"기상청 상징 및 마스코트 '기상이'의 저작권은
기상청에 있습니다"*. 우리 캐릭터가 기상이와 유사하면 문제가 된다(§5 A-4).

## 3. 규정 — 보안 (트랙 04) 및 API 키 (트랙 02)

> **"API 키가 노출되면 실격입니다."**

기상청 키뿐 아니라 **외부 서비스에서 발급받은 어떤 키라도** 동일하다. 금지 4종:
코드에 직접 기입 · **GitHub 저장소에 커밋** · Slack·PR·스크린샷 노출 · 노출된 키 방치.

**키 종류가 둘이다 — 혼동하면 실격 조항에 걸린다.**

| 키 | 용도 | 유효 | 제출 |
|---|---|---|---|
| **대회 제공 키**(대용량 API) | 개발·테스트. **횟수 제한 없음** | **8/22까지** | **절대 제출 금지** |
| **팀 개인 발급 키** | **최종 제출 소스코드·구동 URL** | 자체 | 이것만 사용 |

가이드 CLAUDE.md 예시 원문: *"최종 제출 시에는 반드시 본인 개인 계정으로 발급받은
개인용 API 키를 사용합니다."* (원문 표기가 두 키를 모두 "개인용"으로 불러 혼선이
있다 — §6-Q1로 확인 예정. **실무 결론은 위 표대로 두 키를 분리 운용**한다.)

---

## 4. 우리 계획과의 충돌·영향 12건

### A. 🔴 "로그인 없이 열려야 한다" vs 첫 화면이 로그인 페이지

규정: *"누구나 접속해 **바로 조작**할 수 있는 웹 주소 … **로그인·결제 없이 열려야**"*.
실측: `App.jsx:44` — `if (!accessToken) return <Navigate to="/login" replace />`.
게스트 CTA는 있다(`LoginPage.jsx:12` — *"계정 없이 즉시 체험이 주 동선"*, 게스트 CTA가
첫 시선). 그래도 **심사위원이 처음 보는 화면은 로그인 화면**이고, 규정 문구의 문언적
해석은 "열면 바로 조작"이다.

**결정: 랜딩을 무인증 체험 화면으로 바꾼다.** 최소안은 루트 진입 시 **자동 게스트
발급**(`POST /auth/guest`는 이미 실 유저·실 JWT를 발급한다) 후 학습 홈으로, 로그인·
가입은 상단 링크로 내린다. R13 §2.5(진입 통합)와 같은 파일을 건드리므로 **거기에
편입**한다. 대안(현행 유지 + 데모 녹화에서 클릭 1회)은 규정 리스크를 남기므로 채택
안 한다.

### B. 🟢 프롬프트 세션 — 원재료는 이미 자동 확보돼 있다

**Claude Code가 전 세션을 자동 기록 중이다.** 실측(2026-08-06):
`~/.claude/projects/-Users-...-weathermind/`에 **11세션 · 13,482줄 · 약 46MB**,
기간 **7/07~8/06 전 구간**. 계획서 작성기부터 R13까지 빠진 구간이 없다. 산출물 ③은
"만들어야 할 것"이 아니라 **형식을 맞춰 내보내면 되는 것**이다.

남은 일은 셋뿐이다:
1. **형식** — `.jsonl`은 규정이 요구하는 `.md`/`.txt`가 아니다. 변환 필요.
2. **선별** — 요건이 "**주요** 프롬프트 세션"이다. 46MB 전량은 과다하고, 심사자가
   읽을 수 있는 단위로 라운드별 대표 세션을 고른다.
3. **비밀값 스캔** — §3 실격 조항 방어.

**현시점 스캔 결과: 실키 유출 0건**(2026-08-06 실측). Google(`AIza`+35)·
GitHub(`ghp_`+36)·OpenAI(`sk-`+20↑) 키 형태 **각 0건**, `.env`는 **git 미추적 ·
커밋 이력 없음**, `KMA_API_KEY`도 한글 플레이스홀더라 실키가 아니다. (`sk-` 원시
2,966건은 `task-`·`risk-` 등 단어 내부 오탐, `POSTGRES_USER/DB` 18,333건은 값이
프로젝트명 `weathermind`인 데서 온 오탐이다.)

> ⚠️ **이 안전은 "아직 키를 안 넣었기 때문"이다.** G0·G1·G2에서 실키를 투입하는
> 순간부터 세션에 실값이 섞이기 시작한다 — 스캔은 **8/20 내보내기 직전에 반드시
> 재실행**한다(B-2). 지금 통과했다는 사실은 그때의 보증이 아니다.

### C. 🟡 README 라이선스 인벤토리 부재 + Redis 라이선스 위험

README(96줄)에 아키텍처·실행 순서·API 개요·트러블슈팅은 있으나 **외부 리소스
출처·라이선스 절이 없다**(산출물 ④ 명시 요건). `LICENSE` 파일도 없다.

인벤토리 작성 중 **실제 위험 1건**: `docker-compose.yml:93` **`redis:7-alpine`** —
이 태그는 최신 7.x로 해석되고 **Redis 7.4+는 RSALv2/SSPLv1**(OSI 미승인, GPL보다
제한적)이다. 규정이 "GPL 계열은 활용 범위 제약 가능"이라 경고한 것보다 더 강한
라이선스가 무심코 들어와 있다. **`redis:7.2-alpine`(BSD-3) 핀 고정**을 권한다 —
우리는 Redis를 브로커·캐시로만 쓰므로 기능 손실 0이다.

폰트는 안전하다: `index.css`가 Pretendard를 **선언만 하고 번들하지 않아**
시스템 폰트로 폴백한다(웹폰트 다운로드 없음). README에 그 사실을 적으면 끝난다.

### D. 🟡 소스 zip의 "누락 시 탈락" vs "빌드 산출물 제외"

두 규칙이 반대 방향으로 당긴다: `node_modules`·빌드 산출물은 **빼야** 하고,
**소스코드 누락이 확인되면 탈락**이다. 우리는 4서비스 + 마이그레이션 + 시드 +
스크립트 + docker-compose 구성이라 **"소스"의 경계를 미리 정의**해 두어야 한다.
`.env`는 반드시 제외하되 **`.env.example`은 포함**해야 재현이 가능하다(§5 D-1).

### E. 🟡 URL 유지 기간이 8/21이 아니라 **9월 셋째 주**

ROADMAP §5.5는 8/10 배포~8/22 심사까지만 상정하고 있다. 실제로는 **시상식까지
약 6주 무중단 운영**이다. 클라우드 비용·갱신·모니터링을 그 기간으로 재산정해야 하고,
**8/22 만료되는 대회 제공 키를 쓰고 있으면 심사 이후 URL이 죽는다** — §3의 키 분리가
여기서 두 번째 이유를 얻는다(팀 개인 키는 만료되지 않는다).

### F. 🟡 배포처 자유 — 계획은 유효하나 **가이드가 전제하는 결과물과 우리가 다르다**

*"Vercel, GitHub Pages, Netlify 등 어떤 배포 방식이든 인정됩니다."* → Docker+Caddy
계획은 유효하다. **다만 그 셋에는 우리가 올라가지 않는다**는 것을 정확히 알아 둘 것:
셋 다 **정적 파일 호스팅**이라 상시 구동 서버·Postgres·Redis·Celery beat를 얹을
자리가 없다. 프론트만 올리면 화면은 뜨지만 `POST /auth/guest`부터 전부 404다.
(GitHub Pages는 추가로 **우리 저장소가 private이라 무료 플랜에서 불가**.)

**대회가 전제하는 결과물이 다르다.** 가이드 홈은 "웹 인터랙티브 교육 콘텐츠",
트랙 02 예시는 `index.html` 한 장이다 — 계산이 브라우저에서 끝나므로 무료 정적
호스팅으로 충분하고, **그래서 대회가 인프라를 제공하지 않는 것은 그들 기준에서
모순이 아니다**(제공 대상은 Claude 팀 계정, 즉 만드는 도구까지다).

서버가 필요해진 것은 **우리 선택의 결과**다: 채점을 클라이언트에 두면 결과 주입이
가능해 보드 판정 권위가 무너지고, IRT/BKT 학습자 모델·RLS 유저 격리·리그는 서버
자산이다. 이것이 심사에서 내세울 차별점인 동시에 **리스크 비대칭**을 만든다 —
정적 페이지는 죽지 않지만 **우리는 서버가 죽으면 심사 자체가 성립하지 않는다.**
서버 확보를 8/10이 아니라 **오늘로 당긴 이유가 이것**이고, 막히면 Hetzner
6주 ~€12를 즉시 쓴다(§5 A-1). **비용 문제가 아니라 시간 문제다.**

**가이드의 실제 공백 1건**(→ §6-Q6): 규정은 "API 키 노출 = 실격"인데 정적 페이지가
기상청 API를 부르면 키가 브라우저에 드러난다. 서버리스 프록시가 사실상 필수인데
가이드는 *"키를 직접 쓰지 않습니다"*라고만 하고 대안 경로를 적지 않았다.

**§4-E 때문에 서버는 9월 중순까지 살아 있어야 한다.**

### G. 🟢 코드 프리즈 — 우리 일정이 더 엄격하다

규정 8/21 18:00 vs 우리 8/17 기능 동결. 우리가 나흘 빠르다. **단, 8/18 b 재보정과
8/19~20 QA는 "코드 변경"인지 확인이 필요하다** — b 재보정은 DB의 파라미터 갱신이라
소스 변경이 아니고, QA 중 버그 수정은 소스 변경이다. **8/21 18:00 이후에는 env
조정조차 "핵심 로직 수정"으로 오해받을 수 있으므로 그 시점부터 전면 동결**한다.

### H. 🟡 "제공 계정의 개발 세션 확인" vs 우리는 개인 계정 CLI 개발

규정은 표절 검증과 함께 **제공 계정의 개발 세션**을 확인한다고 적었다. 운영팀 팀
계정은 **본선 진출 15팀에 이메일 초대**로 발급된다. 우리 개발은 사용자 개인 계정의
Claude Code CLI에서 진행됐다. 산출물 ③(세션 내보내기)이 이를 보완하는 구조로 보이나,
**"제공 계정" 세션이 비어 있는 것이 감점 사유인지 불명확**하다 → §6-Q2로 확인.
(팀 계정 초대 수신 여부도 확인 필요.)

### I. 🟡 1366×768 지원 — 최소 해상도 QA 미실시

크롬 3해상도 명시. 우리 스모크 9종은 jsdom·SSR 기준이라 **뷰포트 회귀를 못 본다**.
특히 대기 보드(4구역 지도 + 팔레트 + 단면 패널)와 실험실 슬라이더 화면이 세로
768px에서 잘릴 가능성이 높다. 8/19~20 QA에 **3해상도 실측**을 넣는다.

### J. 🟡 마스코트·이미지 출처 미확인

`frontend/public/` PNG 6종(bolt·cloud·drop·snow·sun·typhoon, 최대 224KB)은
파트너가 8/5에 추가했다. **직접 제작인지 AI 생성인지 외부 소스인지 기록이 없다.**
규정상 "출처 불명 이미지" 금지 + README 출처 표기 필수 + 기상청 '기상이'와의 유사성
회피까지 걸려 있다 → 파트너에게 확인 요청(§5 A-4).

### K. 🟢 대용량 API 필수 아님 — KMA 의존 리스크 완화

*"기획서상 안내는 참고 예시이며, 필수 조건은 아닙니다."* 공공데이터포털·국가기상
위성센터 등도 인정된다. 우리 KMA 키 미발급 리스크가 **"대회 요건 미충족"은 아니라는
것**이 확인됐다(품질 문제로 남을 뿐).

### L. 🟢 표절 방지 — 우리 문서 규율이 이미 요건을 만족

CLAUDE.md의 "대외 문서에 Duolingo 언급 금지, 메커니즘만 차용하고 표현은 자체 제작"
방침이 가이드 CLAUDE.md 예시의 "표절 및 아이디어 중복 방지" 절과 정확히 같은 취지다.
`docs/Observation_Report_02·03`이 저장소 안에 있으므로 **소스 zip에서 제외할지**를
D-1에서 함께 판단한다(벤치마킹 관찰 문서가 심사자에게 노출되는 것은 불리하다).

> ⚠️ **위 문단의 고유명사는 규정 인용이라 존치한다**(CLAUDE.md 금칙 줄과 같은 성격).
> 금칙어를 지운 규정은 검색으로 감시할 수 없다. **2026-08-08 CO-V-3 전수 처리 결과**:
> 규정 인용 2줄(여기 · CLAUDE.md) + 허용처 2곳(Obs02·03) + 이월 대장을 뺀 나머지
> **10파일 14건 → 0건**. 제출물 동결 전 재확인은 `grep -rniE "듀오링고|duolingo"`
> 한 줄로 끝난다 — 잔존이 위 5곳 밖이면 위반이다.

---

## 5. 실행 항목

### A. 구동 URL (산출물 ①) — 기한 8/10 배포 / 9월 중순까지 유지

| ID | 항목 | 소유 | 기한 |
|---|---|---|---|
| A-1 | **Oracle Cloud 가입 + A1 인스턴스 확보**(ap-seoul-1 · **2 OCPU/12GB** — 2026-06-15 축소분) → 확보 즉시 **PAYG 업그레이드**(무과금 유지하며 유휴 회수만 차단) | 클라이언트 | **오늘(8/06) 착수** |
| A-1b | 도메인 확보 — Caddy auto-HTTPS는 실도메인 필요(IP 불가). 무료는 DuckDNS·nip.io. **`.dev`/`.app`은 HSTS preload 강제라 데모용 부적합** | 클라이언트 | 8/09 |
| A-1c | `docker-compose.prod.yml` `build:` → GHCR `image:`(커밋 sha 고정) 전환 — 2 OCPU에서 소스 빌드는 너무 느리다 | DevOps | 8/09 |
| A-2 | 랜딩 무인증화 — 루트 자동 게스트 발급, 로그인·가입은 부차 동선 (R13 §2.5에 편입) | FE | R13 4일차 |
| A-3 | 크롬 1366×768·1920×1080·2560×1440 실측 QA(보드·실험실 우선) | QA | 8/19 |
| A-4 | `public/*.png` 6종 출처 확인 — 직접 제작 여부, 기상이 유사성 | 파트너 | 8/12 |
| A-5 | 최종 배포는 **팀 개인 발급 KMA 키**로 전환(대회 키 8/22 만료) | PM | 8/20 |

### B. 프롬프트 세션 (산출물 ③) — 기한 8/20 (원재료는 이미 확보됨)

| ID | 항목 | 소유 | 기한 |
|---|---|---|---|
| B-1 | `.jsonl` → `.md` 변환 스크립트(`scripts/export_sessions.py`) | PM | 8/19 |
| B-2 | **비밀값 스캔 재실행** — 8/06 실측은 유출 0건이나 **G0~G2 키 투입 후 재검이 필수**. 검출 시 내보내기 중단(§3 실격) | PM | **8/20(내보내기 직전)** |
| B-3 | "주요 세션" 선별 — 라운드별 대표 세션(전량 46MB는 과다) | PM | 8/20 |

### C. README·라이선스 (산출물 ④) — 기한 8/19

| ID | 항목 | 소유 | 기한 |
|---|---|---|---|
| C-1 | README에 **외부 리소스 인벤토리** 절 신설 — 라이브러리·버전·라이선스 / 이미지·폰트 출처 / 데이터 출처 | PM | 8/19 |
| C-2 | **`redis:7-alpine` → `redis:7.2-alpine` 핀 고정**(SSPL 회피, 기능 손실 0) | BE | 8/12 |
| C-3 | 기상청 데이터 **공공누리 유형 확인 + 구체적 출처 표기** | PM | 8/19 |
| C-4 | 폰트 절 — Pretendard 선언만·번들 없음·시스템 폴백 명시 | PM | 8/19 |
| C-5 | 배포 재현 절 — 대회 이후 공공 교육용 실행 가능 수준까지 | DevOps | 8/20 |
| C-6 | **모델·데이터셋 위치 명시** — 시드 JSON·IRT/BKT 파라미터 | PM | 8/19 |

### D. 소스 zip (산출물 ②) — 기한 8/20

| ID | 항목 | 소유 | 기한 |
|---|---|---|---|
| D-1 | 포함/제외 목록 확정 — 제외: `node_modules`·`dist`·`.venv`·`__pycache__`·`.env`·`docs/Observation_Report_*`·**`ai_OT자료/`**(주최측 배포 자료 — 저작권이 우리 것이 아니다. 2026-08-07 감사에서 커밋 혼입 발견, `git rm --cached` + `.gitignore` 처리 완료) / **포함: `.env.example`·마이그레이션·시드·스크립트** | PM | 8/20 |
| D-2 | zip 생성 스크립트 + **압축 해제 후 무키 기동 실증**("누락 시 탈락" 대비) | DevOps | 8/20 |
| D-3 | zip 전수 **비밀값 스캔** | PM | 8/20 |

### E. 일정 규정 반영

- **8/21 18:00 = 전면 동결**(env 조정 포함). ROADMAP §5.5의 8/17 기능 동결은 유지.
- **9월 셋째 주까지 URL 유지** — §5.5 표에 반영 완료.
- 세 산출물(②③④) 마감을 **8/20**로 잡아 8/21 본선 전에 제출을 끝낸다.
- **8/10~8/12 중간점검 멘토링**(§1.3-①) — ROADMAP §5.5 표에 반영 완료(2026-08-08).

### F. 중간점검·발표 (§1.2 산출물 ⓪ · §1.3 — 2026-08-08 신설)

| ID | 항목 | 소유 | 기한 |
|---|---|---|---|
| F-1 | 🔴 **8/07 18:00 중간점검 자료 제출 여부 확인** — 원문에만 있던 마감이라 우리가 못 봤다. 운영팀 이메일·`운영_전체공지`에서 **배포된 발표 양식**과 제출 상태를 확인하고, 미제출이면 즉시 운영 사무국(02-398-4397 / kpc_weather26@kpc.or.kr)에 소명 | 클라이언트 | **즉시** |
| F-2 | 중간점검 희망 일정 회신 — **8/10은 서버 배포·G1 배치일과 겹친다**(ROADMAP §5.5). 가능하면 **8/11~8/12를 희망**해 배포 결과를 들고 들어간다 | 클라이언트 | **8/09** |
| F-3 | 멘토링용 프로토타입 공유 20~25분 구성 — 서사는 `MENTORING_ALIGNMENT.md`(계획서 대비 괴리 4건). 비대면 ZOOM 화면공유라 **로컬 기동이어도 되지만**, 배포 URL이 있으면 그것으로 | PM | 8/10 |
| F-4 | **발표자료(PPT or PDF)** 초안 — 배점 ②(25)·⑤(30) 55점이 화면·시연이므로 슬라이드는 그 둘을 **가리키는** 역할. 수치 문안은 §1.3 대신 `CARRYOVER_R13` U절 「말해도 되는 문안」을 따른다 | PM | 8/19 |
| F-5 | **발표 리허설 — 실측 9분 30초 이내**(§1.3-③ 감점표). 타이머로 2회 이상 | 전원 | 8/20 |
| F-6 | **주최측 PC 전제 점검** — 개인 PC 불가·1일차 19~21시 자료 점검. 크롬 최신 · 확장 없음 · 로그인 상태 없음 · 캐시 없음에서 **URL 한 줄로 완주**되는지 실측 | QA | 8/20 |

**알림은 저장소가 건다** — `.github/workflows/deadline-alert.yml`이 8/17·8/20·8/21
**09:00 KST에 체크리스트 이슈를 자동 생성**한다(cron은 UTC라 `0 0 <일> 8 *`).
대화 세션 기반 알림은 세션이 끝나면 사라져 15일 뒤까지 살아남지 못하므로 워크플로로
걸었다. `workflow_dispatch`로 수동 실행해 동작을 미리 확인할 수 있다.

---

## 6. 운영팀 확인 필요 (Slack `운영_기타문의`)

가이드만으로 판정 불가한 것들이다. **평일 24시간 내 응답** 예정이므로 8/7에 올린다.

> **2026-08-08 갱신 — Q1·Q4는 OT 원문에서 답을 받았다. 올리지 않는다.**
> OT 슬라이드를 열람하지 않은 채 작성된 질문이었다(§1.3 참조). 남은 질문은
> **Q2·Q3·Q5·Q6 4건**이다.

- ~~**Q1. 키 표기 혼선**~~ → ✅ **답 확보 (OT FAQ(Tool), §1.3-④)**. 원문:
  *"기상청 API(대용량)는 개발·테스트하기 위해 **대회 기간에만** 제공됩니다. 단,
  **최종 소스 코드 제출 시점에는 반드시 본인 개인 계정으로 별도 발급받은 API key로
  교체**해야 합니다."* → **§3 표대로 두 키를 분리 운용하는 것이 규정이 요구하는 바다.**
  대회 제공 대용량 키 = 개발 전용·제출 금지 / 팀 자체 발급 키 = 제출용.
  (원문이 명문화한 것은 KMA 키뿐이지만 §3 "노출 = 실격"은 전 키 공통이므로 실무는
  동일 취급 — §1.3-④ 범위 정정 참조.)
- **Q2. "제공 계정의 개발 세션 확인"**(트랙 04) — 팀 계정 발급 이전부터 개인 계정
  CLI로 개발한 이력은 산출물 ③(세션 내보내기)으로 갈음되는가? 팀 계정 세션이
  비어 있는 것이 감점 요인인가?
- **Q3. 구글 폼 주소·마감 시각** — "운영팀 별도 안내"로만 적혀 있다. 제출 마감이
  8/21 18:00과 같은가?
- **Q4. URL 유지 종료일 + 키 만료의 충돌** ⭐ — 제출 유의사항은 URL을 **시상식
  (9월 셋째 주 예상)까지** 유지하라 하고, 트랙 02는 대회 제공 키가 **8/22 만료**라
  한다. 그대로 두면 **심사 직후 URL이 기능 정지**한다. ① 정확한 유지 종료일이
  공지되는가? ② 8/22 이후 구간은 **팀 자체 발급 KMA 키로 교체 운영**하는 것이
  맞는가(=규정이 요구하는 바인가)? ③ 자체 서버(비 Vercel) 배포도 동일 기준인가?
  → 이 답에 따라 §4-E의 서버 비용 산정 기간과 A-5(키 교체 시점)가 확정된다.
- **Q5. 로그인 요건 해석** — 게스트 1클릭 진입도 *"로그인 없이 열려야"*를
  만족하는가? (A-2로 어차피 무인증화하지만, 계정 기능 자체는 남길 예정)
- **Q6. 서버가 필요한 제출물의 인프라** ⭐ — 가이드는 정적 콘텐츠(`index.html`
  한 장 + Vercel)를 전제하는데, 우리 제출물은 **서버 채점 권위·학습자 모델·유저
  격리** 때문에 상시 구동 서버와 DB가 필요하다. ① 이런 풀스택 제출물도 동일하게
  인정되는가? ② **참가팀 대상 인프라(클라우드 크레딧) 지원이 있는가?** ③ 규정은
  "API 키 노출 = 실격"인데 정적 페이지에서 기상청 API를 부르면 키가 브라우저에
  드러난다 — **서버리스 함수·프록시로 키를 숨기는 것이 전제인가?** 가이드에
  그 안내가 없어 초보 참가자는 이 요구를 동시에 만족시킬 방법이 없다.

> Slack 질문은 **모든 팀이 볼 수 있다.** 구체적 구현·디버깅 질문은 평가에 반영될 수
> 있으니 위 5건처럼 **규정 해석만** 올린다(트랙 07 명시).

---

## 7. 커뮤니케이션 채널 (트랙 07)

워크스페이스 `26k-weatherhackathon.slack.com`. **참여는 운영팀 이메일 초대로만** —
직접 가입 신청 불가.

| 묶음 | 채널 | 용도 |
|---|---|---|
| 멘토 | `멘토_교육_api관련` | 교육 콘텐츠 설계, 데이터 API 활용 |
| 멘토 | `멘토_대기_과학` | 대기·기상·기후 개념, 데이터 해석 |
| 멘토 | `멘토_서비스_기획` | 서비스 기획·UX·콘텐츠 구성 |
| 멘토 | `멘토_ai기술_개발` | AI 도구 활용, 개발·구현 방법 |
| 운영 | `운영_전체공지` | 일정·마감·공식 공지 |
| 운영 | `운영_기타문의` | 계정·환경·운영 문의 |

**멘토는 코드를 대신 고쳐주지 않는다** — *"문제 해결 능력도 평가 대상"*. 방향성
질문("이 접근이 맞는지", "어떤 개념을 참고할지")만 유효하다. 응답은 평일 24시간 내.
**스크린샷·로그에 키·비밀값 첨부 금지**(§3).

우리 멘토링 준비물은 `docs/team/MENTORING_ALIGNMENT.md`(계획서 대비 괴리 4건 서사)에
있고, 위 4개 멘토 채널 중 **`멘토_대기_과학`**(문항 과학 정합성)과
**`멘토_교육_api관련`**(R13-0 교육과정 정박)이 우리 현재 작업과 직접 맞물린다.

---

## 8. 규정 인용 시 주의

이 문서는 가이드 원문을 **요약·재구성**한 것이다. 판정이 걸리는 국면에서는 반드시
`https://guide.weatherhackathon.kr`의 해당 트랙 원문을 다시 확인한다 — 가이드는
공지로 갱신될 수 있고, 이 문서는 **2026-08-06 열람 시점의 스냅샷**이다.
