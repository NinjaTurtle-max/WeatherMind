# WeatherMind 팀 개발 종합 회고 (R2 → R5)

> 7직군 서브에이전트 팀 · 애자일 1주 스프린트 · 코드리뷰 · 문서 자산화로 진행한
> R2~R5 5개 스프린트의 종합 회고. 각 스프린트 상세 회고는 SPRINT_R*_01.md §5 참조.

## 1. 완료 범위

| 스프린트 | 커밋 | 산출 | 테스트 |
|---|---|---|---|
| R2 세션·문항 뱅크·품질 게이트·프리즈·레이트리밋 | be26676 | 세션 엔진, 하이브리드 뱅크, 2단 품질 게이트 | 156 |
| R3 대기 보드 퍼즐·플레이 7유형(시뮬레이터 폐지) | 74469d7 | 선언적 규칙 엔진(양측 판정), 채점기 레지스트리 | backend 256 |
| R4 보상 루프(퀘스트·배지·리그 티어·예보 대결) | 67101c5 | 구름 티어, 결정적 AI 캐스터, 미니미션/재현 | backend 326 |
| R5 지도 보드·단계별 학습·구름 에너지 | 9b37f77 | 커리큘럼 트리, 지연 회복 에너지, 지도 UI | backend 370 |
| R3~R5 통합 웨이브2(QA·DevOps) | 8dd2cb8 | 계약 회귀·CI·런북·마운트 정합 | 총 457 |

**제품 방향 전환 2건(클라이언트)**: 기후 시뮬레이터 폐지 → 대기 보드 퍼즐(R3),
지도 기반 + 단계별 학습 + 구름 에너지로 리텐션 강화(R5). 둘 다 관측 보고서 №2의
벤치마킹 설계(§4·§6)를 실행으로 옮긴 것.

## 2. 코드 리뷰가 잡은 결함 (커밋 전 차단)

리뷰가 없었으면 조용히 배포됐을 결함들 — 리뷰 프로세스의 실효를 보여주는 지표:

| 스프린트 | 확정 결함 | 성격 |
|---|---|---|
| R2 | 세션 문항을 /quiz 경로로 제출 시 세션 XP 불일치 | 이원화 |
| R2 | nginx 프록시 뒤 레이트리밋이 전역 버킷으로 수렴 | 배포 환경 |
| R2 | 뱅크 공백 시 LLM 5회 직렬 → 세션 발급 8~15s·단일 실패 503 | 성능·강건성 |
| R3 | SessionItem이 board template 미노출 → 세션 내 보드 렌더 불가 | 기능 간극 |
| R4 | AI↔QA 시그니처 불일치 → 재현 퍼즐 게이트 실패 | 교차 직군 |
| R5 | units.json slug vs 로더 자연키 → **선행 잠금 전면 소실** | 기능 무력화 |

R2는 8관점 병렬 리뷰(후보 33건→확정 10건), R3~R5는 PM 타겟 리뷰(각 직군 자체
테스트가 방어). 컨벤션 위반은 전 스프린트 0건 — 계약 선고정 방식의 효과.

## 3. 무엇이 잘 작동했나

- **계약 선고정 → 병렬 무충돌**: 각 스프린트 시작에 서비스 계약(API 스키마·에러 코드·
  DB DDL·시드 스키마)을 §3에 고정하고 4직군이 동시 작업 → 디렉토리 소유권 충돌 0건.
- **단일 진실원 강제**: 보드 규칙(board_rules.json)을 프론트·백엔드가 같은 파일로 해석,
  공유 테스트 벡터(board_test_vectors.json)로 양측 판정 일치를 CI가 상시 보증.
  시드 계약 테스트가 ai-worker 검증기를 실제 임포트(재기술 금지).
- **결정적 폴백 설계**: 품질 게이트·AI 캐스터를 LLM 없이도 동작하게 만들어 키 부재
  환경에서도 전 테스트 실행 가능(457건 전부 DB·LLM 불필요).
- **문서 자산 누적**: TEAM_PROCESS·스프린트 계약·리뷰 노트·회고·런북·콘텐츠 가이드가
  다음 라운드의 컨텍스트로 재사용 — 세션 중단·재개에도 계약이 손실을 방지.

## 4. 무엇이 아쉬웠나 (개선 액션)

- **시드 스키마 미고정이 통합 결함을 낳음(R5)**: API·DDL은 고정했으나 시드 파일 필드
  스키마를 계약에 안 박아 3직군이 다른 해석. → **이후 계약은 시드 파일 필드까지 명시**
  (R5 §3.2 갱신 완료). "공유되는 모든 것에 계약"이 교훈.
- **교차 직군 소유 경계의 마찰(R4)**: celery_app(DevOps)·test(QA)·validator(AI)가 얽힌
  시그니처 변경이 결함. → 공유 시그니처 변경 시 소비자 목록을 계약에 명시.
- **세션 사용량 한도로 에이전트 다회 중단**: 재개 체계(SendMessage로 transcript 복원)로
  산출 손실은 0이었으나 벽시계 시간 증가. 큰 작업은 웨이브 분할 유지.

## 5. 이월 부채 · 로드맵 (미구현 명시)

- **실기동 통합 테스트**: 전 테스트가 순수함수/소스텍스트 기반. docker compose 실기동 +
  실제 KMA/Gemini 키로 INTEGRATION_CHECKLIST 실행은 키 발급 후(체크리스트 준비 완료).
- **로드맵 유지**: 기후 탐정 스토리·AI 캐스터 롤플레이(LLM 키), 웹푸시(구름 회복 알림),
  WeatherBrain IRT 재학습(사용자 데이터 축적 후 — 콜드스타트는 weak_tags·규칙으로 동작).
- **경미 부채 — R5.5 정리 완료(2026-07-23)**: 아래 4건을 독립 커밋으로 해소.
  - ai-worker conftest.py 부재(bare pytest 수집 실패) → conftest 추가(1b967e8).
  - 밸런스 튜닝(구름 비율·세션 문항 수) → Settings/env 외부화, 기본=계약수치(13e46fc).
  - ai-worker 파싱 로직 공용화 → json_output.extract_json_object로 단일화(7246989).
  - KMA 카테고리 해석 3원화 → 같은 컨텍스트 DRY(SKY_TEXT) + 교차 컨텍스트 계약
    테스트(test_kma_contract)로 분해 해소(8725877). 빌드 컨텍스트 재구성은 회피.
  교훈: 교차 빌드 컨텍스트의 "공용화"는 물리적 병합이 아니라 단일 진실원 + 계약
  테스트를 뜻한다(board_rules 관례). backend 422 · ai-worker 59 통과.

## 6. 지표 요약

- 커밋 8개(베이스라인 + 5스프린트 + 사용자 1), 소스 파일 ~161개.
- 자동화 테스트 457건(backend 404 · ai-worker 53), P0~P2 결함 0건.
- 플레이 유형 3 → 10종(객관식·단답·슬라이더 + board·match·ordering·cloze + 미니미션·재현).
- DB 마이그레이션 0001 → 0005, 시드 47문항 + 12유닛 + 5배지 + 8규칙.

---

# R10-01 회고 (2026-08-01 ~ 08-03)

> R6~R9 회고는 각 `SPRINT_R*_01.md` 하단 참조. 이 절은 **R10-01** 단독 회고다.
> 근거: 실사용 필드 테스트(`Observation_Report_03`) → 웨이브 0(계약) → 웨이브 1(구현
> 병렬 5) → S5 → S6(통합). PR #14·#15 병합, S6 브랜치 진행.

## R10.1 완료 범위

| 단계 | 산출 | 검증 |
|---|---|---|
| 웨이브 0 | 계약 테스트 **78건을 빨간 상태로 선커밋** · 파일 소유 경계 물리 분리(`PeninsulaMap` 추출) · mock 선반영 · 결정 기록 D1~D10 | 기존 761 회귀 0 |
| 웨이브 1 | S1 에너지 정책 전환 · S2 WebGL 단면 8종 · S3 지도 오버레이 · S4 온보딩 목표+점진 해제 | 78건 → **전부 초록** |
| S5 | 언두 · 점진적 힌트 2단 · 이탈 인텐트 · 콤보 · 마감 4건 | 프론트 스모크 신설 |
| S6 | **실DB 왕복 스모크**(미검증 3건 검증) · 테스트 공백 4건 가드화 · CI 프론트 9종 편입 | backend **897** · 프론트 **9종** |

**P0 2건 해소**: 구름이 정답 제출까지 차단하던 문제(→ 오답에만 소모, 진입 전 차단,
진행 중 세션은 잔량 0에도 보장) · 첫날 세션 중복(→ review·live·유닛 풀에 당일 응답 제외).

## R10.2 리뷰·검증이 잡은 결함 (전부 배포 전 차단)

이번 라운드의 가장 큰 성과는 기능이 아니라 **잡아낸 것들**이다.

| 단계 | 결함 | 성격 |
|---|---|---|
| 웨이브 0 | 계약(§3.1)이 차단 지점으로 지목한 보드 상세 엔드포인트가 **실재하지 않음** | 계약↔코드 불일치 |
| 웨이브 0 | 첫날 중복 P0의 원인이 **뱅크 규모가 아니라 `review`·`live` 풀의 쿼리 누락** | 오진단 — 스프린트 우선순위 근거가 뒤집혔다 |
| 웨이브 0 | 지도 렌더러가 조작 파일 안에 있어 **3인이 같은 파일을 동시 수정**하는 구조 | 편성 전제 붕괴 |
| 웨이브 0 | 유닛 풀에 하드 제외를 걸면 **0문항 세션 발급**(생성 폴백 부재) | 개선이 회귀를 만드는 케이스 |
| 웨이브 1 리뷰 | 진입 게이트를 서버·mock에 만들었으나 **프론트가 호출하지 않아 잔량 0에서 보드 무제한** | 계약 미이행 + R10 이전보다 약해진 회귀 |
| S5 리뷰 | 이탈 인텐트 센티널이 **세션마다 누적**(뒤로가기가 세션 수만큼 헛돎) | 누적 부채 |
| S5 | 약점 보너스 XP를 프론트가 **백엔드 상수 사본으로 역산** | 조용한 드리프트 |
| S6 | `smoke.sh`가 head를 하드코딩해 **마이그레이션이 정상인데 FAIL** | 게이트 무력화 |
| S6 | **RLS가 런타임에 무효**(앱 롤이 `bypassrls` 슈퍼유저 + 테이블 소유자) | 문서↔실동작 불일치 |

## R10.3 무엇이 잘 작동했나

- **계약 선고정을 "빨간 테스트"로 구현했다.** 문서에 적는 대신 실패하는 테스트 78건을
  먼저 커밋해, 웨이브 1의 완료 판정이 주장이 아니라 초록/빨강이 됐다. 구현자가 계약을
  다르게 해석할 여지가 사라졌다.
- **mock 선반영(D5)이 병렬을 실제로 가능하게 했다.** 프론트 3인이 백엔드 구현을
  기다리지 않았고, FE-3은 서버 계약이 착지하기 전에 목표·게이팅을 완성했다.
- **에이전트가 PM 지시의 결함을 실측으로 반박한 사례 3건.** 이게 없었으면 그대로
  틀린 것이 들어갔다:
  - FE-2: `boardLayout.js` 생성 지시에 "양쪽 공유 심볼 0개" 근거표로 반박 → 대신 진짜
    결합(좌표 데이터/사영 분리)을 찾아냄
  - FE-4: 센티널 회수 지시가 불충분함을 발견(`history.back()`이 비동기 큐라 레이스) →
    변이 검증으로 "지시안만으로는 가드가 실패한다"를 증명
  - SA-1: "주석 수정 유지 + 앵커 좁히기" 지시의 자기모순을 지적하고 근본 수정을 선택
- **변이 검증이 관례로 정착했다.** 가드를 만들면 그 가드가 **실제로 잡는지** 대상을
  일부러 어긋나게 해서 확인한다(S5 센티널 2건, S6 WebGL·오버레이 4건, 보드 진입 1건).
  통과 자체보다 "실패해야 할 때 실패하는가"가 판정 기준이 됐다.

## R10.4 무엇이 아쉬웠나 (개선 액션 — 전부 반영 완료)

1. **워크트리 격리 규칙을 내가 세워놓고 지키지 않아 사고가 났다.** 병렬 5인 공유 트리에서
   `git stash -u`가 전원 작업분을 되돌렸다(백업으로 손실 0). 원인은 규칙이 **인원 기준으로
   애매하고 방법이 없었던** 것. → **T0~T2 등급(계약 성숙도 기준) + 실측 기반 방법**을
   §2.6.1로 명문화. S6에서 실제 적용해 병렬 2인 무사고 완료.
2. **계약을 "라우트 존재"로만 박아 P0가 초록 테스트 뒤에 숨었다.** 서버·mock·테스트가
   전부 통과하는데 사용자 경로가 연결되지 않았다. → **프론트가 계약 엔드포인트를 실제로
   호출하는지 요청 관측으로 검증하는 가드 유형**을 추가(`boardEntryGate.smoke`).
3. **검증을 일회성 하네스로 끝내 상주 가드가 없었다**(4건). 드로우콜 예산·`SCENES` 정합·
   오버레이 순수 모듈·`FLOW_META` 사본이 모두 "확인은 했지만 회귀는 못 잡는" 상태였다.
   → S6에서 전부 가드화. **앞으로는 검증한 즉시 가드로 승격**한다.
4. **문서가 3라운드분 쌓여 중복·모순이 생겼다.** 6단계 로드맵이 저장소 밖(에이전트 메모리)에
   있고 스프린트 문서가 그것을 SSOT로 가리키는 역전 구조였으며, `CLAUDE.md`는 "θ→난이도
   미연결"처럼 뒤집힌 서술을 들고 있었다. → `docs/ROADMAP.md` 신설(전략 SSOT 저장소 이관),
   용어 규약(마일스톤/항목/스토리), `CLAUDE.md` 감사.
5. **스프린트 문서 §3이 실제 코드와 4곳 어긋났다.** → 웨이브 0 결정 기록(§4.1 D1~D10)이
   **§3보다 우선**한다는 규약을 문서 자체에 명시.

## R10.5 이월 (미검증·미배정 명시)

- **실DB 미검증 잔여**: 동시성/경합(가드 UPDATE lost update) · KST 자정 경계 ·
  `CLOUD_COST≥2` 환경 · 이탈 인텐트 실브라우저 수기 확인.
- **마일스톤 5 최우선**: 앱 전용 비특권 DB 롤 분리(RLS 런타임 무효 — ROADMAP §2.1).
- **미배정 항목**: R10-I·J·K·L·M·O·P·Q(관찰 보고서 §3·§3.1·§3.2-4). R10-J는 P1.
- **임계 경로**: Gemini 키 — 마일스톤 3 뱅크 확장 + 다국어 문항 + 기초과학 파일럿
  3트랙이 동시에 대기(ROADMAP §5.3, 기한 8/08).
- `bcrypt`↔`passlib` 비호환(R6 발견)은 이미지 재빌드 시 재발 가능 — requirements 핀 미적용.

## R10.6 지표

- 자동화 테스트 **backend 897**(웨이브 0 시점 761 → +136) · 프론트 스모크 **9종**
  (visual·board·session·placement·gating·board-entry·assist·webgl·overlay) ·
  ai-worker 86+1skip. **P0~P2 미해결 0건**.
- DB 마이그레이션 0007 → **0008**, 시드에 `hint_needs` 8종 additive.
- WebGL 2종 신설(단면 1,528줄 · 오버레이 992줄), 외부 라이브러리 **0**,
  메인 번들 순증 858.85 kB(lazy 청크 분리 후).
- 실DB 왕복 스모크 7단계 신설(`scripts/smoke_r10.sh`), 연속 3회 그린.

## R10.7 웨이브 2 종료 (2026-08-03) — 통합·상주화

**편성**: PM 1인 + DO-1(CI 상주화) 1인. §2.6 기준 증원 대상 아니었음 — 도커 재빌드가
동시 편집과 충돌하므로 병렬 폭을 의도적으로 좁혔고, `.github/workflows/`만 파일 소유가
완전히 분리돼 유일한 병렬 대상이었다.

**산출물** (브랜치 `chore/r10-06-integration`)
- 도커 8서비스 재빌드·기동(R9 미반영 이미지 해소) → **실기동 스모크 7단계 전원 OK**.
- **CI 상주화** — `.github/workflows/`가 아예 없었다. `scripts/ci.sh` 5단계를 잡으로
  나눠 PR·push 게이트로 세웠다. 검사 로직은 재구현하지 않고 ci.sh를 호출한다.
- 스모크 4단계를 3단계 유저에서 분리 — 배합 운에 걸리던 FAIL 제거.
- **목의 하루 경계를 KST로 정렬** — D9 이월 항목 해소.

### 웨이브 2가 잡은 것 3건 (전부 "그린인데 무의미"류)

1. **DO-1의 SKIP 방어가 board 잡을 상시 실패시켰다.** `grep -q 'SKIP'`이 상태 컬럼과
   설명문을 구분하지 못하고, board의 **성공** 비고가 실제로 "(또는 시드 부재 시 자체
   SKIP)"이다. PM 검증에서 오탐을 실측(`결과=OK`인데 `SKIP포함=1`)해 상태 컬럼 고정
   매칭으로 교체. → **교훈: 게이트를 문자열 검색으로 만들면 게이트가 데이터에 걸린다.**
   에이전트 보고를 실행 출력으로 확인하는 규약이 값을 한 것이 이 건이다.
2. **스모크 4단계 성공이 세션 배합 운에 의존했다.** 첫 실행에서 배합이
   `mc,board,board,mc,board`로 나와 3단계가 비board를 전부 소진 → 4단계 FAIL.
   제품 결함이 아니라 하네스 취약점. 조건을 만족하는 세션을 직접 확보하도록 고쳤다.
   → **교훈: 스모크 단계 간 상태 공유는 편의가 아니라 결합이다.**
3. **CLAUDE.md의 프론트 스모크 9종 목록이 틀렸다.** `explore`가 빠지고 `board`가
   들어가 있었다. 실측: `FRONT_TESTS`는 explore·session·placement·visual·gating·
   board-entry·assist·webgl·overlay 9종이고 `board`(board_engine 벡터)는 **별도 단계**다.
   `test:*` 10종 전부가 CI에 걸려 있음은 확인됐다 — 누락된 테스트는 없고 표기만 틀렸다.

### D9 이월 항목 처리

- **목 UTC/KST 9시간 시프트 — 해소.** D9는 "`session_date`·`quiz_id` 채번까지 파급"을
  우려해 웨이브 0에서 손대지 않았으나, 실측하니 날짜 계산이 `todayISO()` **단일
  진입점**에서만 파생돼 파급 범위가 닫혀 있었다. 계약 + 변이 검증으로 고정.
  → **교훈: "파급이 클 것 같다"는 추정이 이월 근거가 되면 부채가 실측 없이 연장된다.**
- `preview` vs `zoneVisuals` 이중 진실원 · 유닛 세션 멱등 재조회 부재 — R11 이후 유지.

### 웨이브 2 발견 — board 과다 출제 (범위 밖, 마일스톤 3으로 이월)

스모크 4단계가 배합 운에 걸린 것을 계기로 실측했더니 **출제 비중이 시드 비중과
어긋난다**(당일 daily 세션 전량 130문항 집계).

| 유형 | 시드 비중 | 실제 출제 | 배율 |
|---|---|---|---|
| board | 22.6% (12/53) | **30.8%** (40/130) | **1.36×** |
| multiple_choice | 39.6% (21/53) | 30.0% (39/130) | 0.76× |

`enforce_type_variety`는 **같은 유형 3연속만** 막고 전체 비율은 보지 않는다. 실제
세션 배합이 `mc,board,board,mc,board`·`board,short_answer,board,board,mc`처럼
board 3/5로 나온 표본이 연달아 나온 것이 이 편향의 표면이다.

사용자가 R10 실사용에서 지적한 "보드량" 체감과 방향이 일치한다 — 즉 체감이 아니라
측정되는 편향이다. 다만 원인 규명(풀 선정이 board를 선호하는지, 실황 슬롯이 board로
채워지는지)과 수정은 **마일스톤 3의 배합·난이도 작업 범위**이므로 웨이브 2에서
고치지 않고 근거만 남긴다.

## R10.8 지표 (웨이브 2 종료 시점)

- 자동화 테스트 **backend 937**(웨이브 0 시점 761 → +176) · **ai-worker 97** ·
  프론트 스모크 **9종 + board 벡터 1** = `test:*` 10종 전부 CI 편입. P0~P2 미해결 0건.
- **CI 상주화 완료** — 이전까지 게이트는 "PM이 기억하면 도는" 상태였다.
- 실기동 스모크 7단계 전원 OK(도커 재빌드 후 실측).
