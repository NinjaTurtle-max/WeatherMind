# 8/20 롤링 범위 — 가지치기 (사용자 확정 대기)

**작성** 2026-08-19 · PM 세션 · **확정 권한은 사용자**
**전제** 8/20 롤링 · **8/21 18:00 전면 동결**(배포 기준) → 실질 작업 가능 시간 **1일**

---

## 0. 판별 기준

가치가 아니라 **「8/20까지 착지 가능 + 남은 손이 있는가」**로 잘랐다.

| 손 | 지금 물고 있는 것 | 여유 |
|---|---|---|
| A조 | ⑪-b(아이콘 잘림, 브라우저 확인) | 반나절 |
| B조 | 갤러리 → ⑫(불러오기 로그인화) | **없음** |
| PM | ⑩ 완료 · 홍수 실화면 검증 · 게이트 큐 | 반나절 |

⇒ **신규 착수는 최대 2건**이 현실이다. 3건을 넣으면 전부 반쯤 된 상태로 동결된다.

---

## 1. 🔴 먼저 — 목록 중 **이미 있는 것**이 절반이다

사용자 목록을 실측한 결과다. **이미 있는 것을 다시 배정하면 하루를 날린다.**

| 목록 항목 | 실측 | 자리 |
|---|---|---|
| 위성지도 | ✅ **있다** | `modules/explore/SatelliteView.jsx` + `satelliteField.js` |
| 시뮬레이션 3D | ✅ **있다** | `board/webgl/crossSection/` — **GL 20종 + SVG 폴백 20종** |
| 일기도 등 보드 추가 | ✅ **PR #131** | 일기도 판독 2판 + 대기역학 → 보드 57판 |
| 탐구 활동(태풍·기후변화) | ✅ **있다** | `TyphoonSimPage` · `ClimateSimPage` · `SandboxPage` |
| 난이도 표기를 교과 과정 기준으로 | ⚠️ **부분** — §3 참조 | 10단계 축은 됐다(「초등 3~4학년」…「기상청 현업」 + 부제) · 보드는 #32b 전환 완료. **그런데 2화면이 아직 초급/중급/고급이다** |
| 초등학생 대상 난이도 조절 | ✅ **PR #133** | 초등 톤 |
| 문항 난이도를 순차적으로 | ✅ **PR #136·#138** | 열린 구간 **최상위**부터 시작 + 「인정/푼 것」 분리 |
| 배포 안정성 확보 | ✅ 신규 아님 | 롤링 검증 그 자체 |

⚠️ **이 8건은 「버릴 것」이 아니라 「제출물에 올릴 것」이다.** 심사위원이 못 본 기능은
0점이다. 시연 영상·README 목록으로 **8/20 제출물 작업에 넘긴다.**

---

## 2. ✅ IN — 내일 롤링에 넣는다 (신규 2건)

### ⑬ 탐구 조작 변수 보강 — **최우선**

**배점 근거**: 체험·참여형 10점 항목 원문이 *"변수를 바꿔가며 학습 탐구가 가능한가"*.

**실측**: 화면은 전부 있는데 **변수가 얇다**.

| 화면 | 조작 변수 | 목표 |
|---|---|---|
| 태풍 | **3** (해수면온도 슬라이더 + 시어 선택 + `SatelliteView` 슬라이더) | 그대로 |
| **기후변화** | **1** | **3** (+2) |
| 위성 | 1 | 손대지 않음(태풍 화면 안에서 함께 쓰임) |
| 자유 보드 | 보드 자체 상호작용 | — |

⚠️ **PM 첫 실측이 틀렸다**(A조 정정, 2026-08-19). 태풍을 2로 셌는데 **3**이다 —
`TyphoonSimPage`가 `SatelliteView`를 렌더하고 그것이 자기 슬라이더를 갖는다.
파일 단위로 세면 **공유 컴포넌트로 들어오는 변수를 놓친다.** 세는 법: `type="range"` ·
`<select>` · radio · `*OPTIONS` 상수 + **컴포넌트 사용까지 따라가고, 주석을 걷고** 센다.

**할 일**: **기후변화 한 곳에 손을 모은다**(1 → 3). 태풍은 이미 3이고 시어 선택이
「바꿔가며」를 성립시킨다. 두 화면을 얕게 건드리는 것보다 한 화면을 제대로 하는 편이
낫고, 남은 시간이 하루다.

**AC**: 값을 움직이면 **결과가 즉시 화면에 보여야** 한다. 숫자만 바뀌고 그림이
안 바뀌면 배점 항목이 요구하는 「탐구」가 아니다.
⚠️ **jsdom에 래스터라이저가 없어 「그려진다」는 원리적으로 측정 불가**다(#132가 그
한계를 파일 머리에 명시했다). 측정 가능한 선까지만 물리고 **못 무는 것은 못 문다고
적는다** — 「그림이 바뀐다」를 단정한 척하는 계약이 더 나쁘다.

**소유** A조(⑪-b 뒤) · **반나절** · 엔진 확장 불요(기존 파생식에 입력만 추가)

### ⑭ 산불 모식도 개선

**배점 근거**: 독창성 8점 항목 원문이 *"산불-홍수 모식도"* **쌍**인데 홍수만 끝났다.

**대상 2장**: `wildfire_risk_dry_gale` · `siberian_gale_wildfire`
**할 일**: 홍수에서 확립한 패턴 그대로 — **깊이/규모를 잴 물건**(홍수의 차 = 산불의 수관·건물)
+ 지형 층화 + 오독 차단(화선이 바람 방향과 어긋나 보이지 않게).
**소유** PM · **반나절** · 조사 문서 동봉(홍수와 동일 절차 — 복제·트레이싱 금지)

---

## 3. ❌ OUT — 사유

| 항목 | 뺀 사유 |
|---|---|
| 경계층 수치 모델 보드 | **엔진 확장** 필요. 이미 사장님 판정 대기 버킷 |
| API 훈련 고도화 | 비용 게이트 G1·G2 + **키 미포함 제출**과 충돌(심사 중 안 돌아감) |
| 과학적 정합성 체크 · 자료·설명 검증 | **사용자 지시로 배제**(QA 항목) |
| 문항 난이도 전면 재구성 | 1,000건 재검 — 하루 불가 |
| 대상별 교과목 구분 | **태그×단계 행렬 72칸 중 31칸만 참** → 구조 재판정이 선행 |
| 사용자 대상 전체 학습 활동 구성 | 범위 불명 + 위 항목에 종속 |
| 초등~성인 세심한 조절 | ⑬⑭ 뒤 남는 손이 없다. 실운영 관측 없이 조정하면 근거가 없다 |
| **난이도 표기 잔존 2화면** | **신규 2건 상한**으로 제외. ⚠️ **⑭와 교환 가능** — 아래 참조 |

### ⚠️ 난이도 표기 잔존 — 「됐다」가 아니다

§1에서 「대부분 됐다」로 적었으나 **실측하면 두 화면이 아직 범용 밴드**를 그린다.
사용자가 지목한 *"쉬움/보통/어려움 대신 교과 과정 기준으로"*가 바로 이 유형이다.

| 자리 | 그리는 것 |
|---|---|
| `modules/progress/AbilityRadar.jsx:86` | `ability.level.{level_label}` → **초급/중급/고급** |
| `modules/progress/WeatherBrainPanel.jsx:148·233` | `weatherBrain.mastery.*` 칩 + `LEVEL_KO` → **초급/중급/고급/최상급** |

**둘 다 `/me`(내 정보)에 있다 — 심사위원이 여는 화면이다.**

서버 스키마(`schemas/progress.py:186`)는 *"level_label을 대체하지 않는다 — 두 축이
공존한다"*고 **의도적으로** 적혀 있어 결함은 아니다. 하지만 사용자 요구는 「표기를
교과 기준으로」이므로 **요구 미충족이 맞다.**

**비용은 낮다**: 개념별 `knowledge_level`이 이미 응답에 내려오고(`schemas/progress.py:233`),
단계→부제 매핑도 이미 있다(`lib/abilityDisplay.js:114`). **표시만 교체**하면 되고
반나절이다.

⇒ **⑭ 산불 모식도와 교환 가능하다.** 배점으로 보면 이쪽이 학습효과 5점 항목
(*"대상에 적합한 난이도인가"*)에 직접 닿고, ⑭는 독창성 8점 항목의 절반(홍수는 이미
끝났다)이다. **어느 쪽을 넣을지 정해 주십시오** — 둘 다는 신규 3건이 되어 상한을 넘는다.
| 선지 길이 편향 | 이월 유지 |

---

## 4. 함께 나가는 대기분 (신규 아님)

**#130** 문서 · **#131** 보드 2판 · **#132** 태풍 백지 · **#133** 초등 톤 · **#141** 이월 · **#142** MT-22

✅ **PM 몫 2건 — 둘 다 처리했다**(2026-08-19)

**#132 본문 변이 표** — 「V5 exit 0 · 전건 초록」을 갱신했다. B조 `4bc28aa`를 별도
워크트리로 떼어 **직접 변이**했다: exit 1 · FAIL 9건. 그중 **⑦ 계열 3건**(⑦-0b·⑦·⑦-c)이
V5가 새로 지키는 것이고, 나머지 6건은 종전에 이미 덮여 있던 전(全) null 경로다.
표에는 **3건**을 적었다 — 총수를 적으면 이 가드의 기여를 과장한다. 그리고 담당이
*"⑦-b는 V5를 못 잡는다"*고 밝힌 것도 **실측으로 맞았다**(⑦-b는 실패 목록에 없다).

**낡은 숫자** — A조 보고 6곳을 **내용으로 다시 세니 10곳**이었고, 그중 2곳은
「장면 v1 (도입 당시 8종)」이라 **고치면 안 되는 이력 표기**였다. 줄 번호로 찾으면
또 낡은 값을 인용한다.
🔴 **「20종」으로 갱신하지 않았다** — #131이 규칙을 늘리는 순간 또 낡는다. 수를
**산문에서 뺐다**: 현재 총수를 주장하던 자리 → 「전 규칙」(SSOT는 `board_rules.json`),
실패 메시지 → `${ruleKeys.length}`에서 파생. 이력 표기는 「그때의 증분」이라 명시해 남겼다.
변이 확인: GL 장면 1종 삭제 → 「규칙 **20종** / 누락: greenhouse_tropical_night」로
**바른 규모**를 말한다. `fix/flood-city-submerged` @ `63b9703`.

---

## 5. 확정이 필요한 것

1. **신규 2건을 무엇으로 할지** — ⑬(탐구 변수)는 고정. 둘째 자리를 **⑭ 산불 모식도**와
   **난이도 표기 잔존 2화면** 중 어느 것으로 할지. **둘 다는 상한 초과다.**
2. **경계층 보드** — 넣을지 뺄지(엔진 확장이라 8/20은 불가, 판정만)
3. **§1의 7건을 제출물 목록으로 넘길지** — 시연 영상·README 작업이 여기서 갈린다

## 6. ⚠️ 동결 전 리스크 — ⑫

**⑫(불러오기 로그인화 + 대문 복귀)가 아직 미착수**다. B조가 갤러리 뒤로 두었고,
갤러리는 **실브라우저 렌더 미확인 2건**(홍수·MT-22)의 유일한 검증 수단이라 순서
자체는 맞다. 다만 ⑫는 **사용자가 직접 지시한 결함 수정**이므로, 갤러리가 길어질
조짐이 보이면 **끝내지 말고 즉시 보고**하도록 B조에 규칙을 주었다. 둘 다 못 하게
되면 **⑫가 먼저**다.
