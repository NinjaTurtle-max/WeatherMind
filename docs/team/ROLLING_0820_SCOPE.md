# 8/20 롤링 범위 — **확정** (2026-08-19)

**클라이언트 지시**: *"두개 팀 구성으로 분할 진행 시켜, 그리고 너는 가지를 쳐야지 이제"*

⇒ **이 문서가 SSOT다.** 신규 착수는 아래 2건뿐이고 **나머지는 잘랐다.**
질의는 끝났다 — PM이 결정하고 여기 적는다.

**동결** 2026-08-21 18:00 (배포 기준) · **롤링** 8/20 · **실작업 잔여 ≈ 1일**

---

## §1 두 팀 분할

🔴 **2026-08-19 클라이언트 정정**: *"보강하려던 ⑬을 제가 잘랐습니다 — 다시해.
왜 너가 판단해서 잘라"* · *"`/me` 두 화면이 아직 초급/중급/고급입니다 — 팀에 붙여"*

⇒ **「팀당 신규 1건」은 PM이 스스로 만든 제약이었고 철회한다.** 가지치기는
**후보를 실행 가능한 것으로 좁히는 일**이고, **배점 근거가 가장 강한 항목을
PM 판단으로 떨어뜨리는 일이 아니다.** ⑬은 되살리고 난이도 표기도 붙인다.

**배정 기준은 소유 충돌이다** — A팀이 `i18n/resources/ko.js`·`en.js`를 배타
소유로 잡았으므로 **새 i18n 키가 필요한 것은 A팀**, 필요 없는 것은 B팀이 맡는다.

### A팀 (`de1461`) — 「모르겠어요」 버튼

**지시 원문**: *"배치고사에 문제마다 모르겠어요 버튼을 만들어 문제 건너 뛰도록하자
그러고 모르겠다 한것은 틀린 것으로 모델에 할"*

| | |
|---|---|
| **범위** | 배치고사 문항마다 「모르겠어요」 → 건너뛰기 · 그 문항은 **오답으로 θ에 반영** |
| **함께** | **⑬ 탐구 조작 변수**(아래) · ⓑ ⚡ VS16(진행 중) · §5.25 대장(A조 소유) |
| **⚠️ 필수** | **목(`apiMockPlugin.js`) 동기화.** 안 하면 §5.20ⓓ 재발 — 목이 낡으면 **스모크가 결함 상태를 계약으로 굳힌다** |
| **기점** | **`origin/fix/unit-unlock-on-complete`** — 목을 PM이 이미 고쳤다(#143). main에서 갈라 오면 충돌 |
| **착수 전** | θ/IRT 소유 직접 확인(`git log --all --since=2026-08-18 -- backend/app/services/weatherbrain_service.py backend/app/services/placement_service.py`). **PM 말을 근거로 삼지 말 것** |

#### A팀 추가 — ⑬ 탐구 조작 변수 (되살림)

**배점 근거**: 체험·참여형 25점 중 **10점 항목 원문** — *"단순 퀴즈·정답 맞히기를
넘어 **변수를 바꿔가며** 학습 탐구가 가능한가"*.

| 화면 | 실측 조작 변수 | 목표 |
|---|---|---|
| 태풍 | **3**(해수면온도 + 시어 선택 + `SatelliteView` 슬라이더) | 그대로 |
| **기후변화** | **1** | **3** (+2) |
| 위성 | 1 | 손대지 않음(태풍 화면 안에서 함께 쓰임) |

**기후 한 곳에 손을 모은다.** 축이 하나면 「바꿔가며」가 성립하지 않는다 —
비교가 안 된다. 태풍은 이미 3이고 시어 선택이 그 조건을 만족한다.

**AC**: 값을 움직이면 **결과가 즉시 화면에 보여야** 한다. 숫자만 바뀌고 그림이
안 바뀌면 배점 항목이 요구하는 「탐구」가 아니다.
⚠️ **jsdom에 래스터라이저가 없어 「그려진다」는 원리적으로 측정 불가**다(#132가 그
한계를 파일 머리에 명시). 측정 가능한 선까지만 물고 **못 무는 것은 못 문다고 적을 것.**
**범위 밖**: 엔진 확장·새 파생식. 기존 파생식에 **입력만 추가**하는 선까지.

**A팀인 이유**: 새 슬라이더 라벨 = **새 i18n 키**가 필요하고 `ko.js`·`en.js`는
A팀 배타 소유다. 그리고 사전 실측(위 표·세는 법)을 A조가 이미 해 두었다.

---

### B팀 (`693496`) — ⑫ 불러오기 로그인화 + 대문 복귀

| | |
|---|---|
| **범위** | 진도 불러오기를 로그인 기반으로 · 1차 접속 이후 대문 복귀 화면 |
| **함께** | 태풍·복사수지 라벨 가독성(진행 중) |
| **기점** | **#134 위에** 쌓기 |
| **함께** | **난이도 표기 잔존 2화면**(아래) |
| **순서** | 라벨 → **⑫** → 난이도 표기. **막히면 끝내지 말고 즉시 보고** |

#### B팀 추가 — 난이도 표기 잔존 2화면

🔴 **클라이언트 직접 지시**: *"`/me` 두 화면이 아직 초급/중급/고급입니다 — 팀에 붙여"*

| 자리 | 지금 그리는 것 |
|---|---|
| `modules/progress/AbilityRadar.jsx:86` | `ability.level.{level_label}` → **초급/중급/고급** |
| `modules/progress/WeatherBrainPanel.jsx:148·233` | `weatherBrain.mastery.*` 칩 + `LEVEL_KO` → **초급/중급/고급/최상급** |

**둘 다 `/me`에 있다 — 심사위원이 여는 화면이다.** 10단계 교과 표기와 4밴드
범용 표기가 **한 화면에 공존**한다.

**필요한 것이 이미 다 있다**:
· 개념별 `knowledge_level`이 응답에 내려온다(`schemas/progress.py:204·233`)
· 단계→이름·부제 매핑이 있다(`lib/abilityDisplay.js` — `KNOWLEDGE_LEVEL_SUB`,
  리소스 `ability.knowledgeLevel.name`/`.sub`)
⇒ **새 i18n 키 0건. 표시만 교체한다** — 그래서 A팀의 i18n 배타 소유와 충돌하지 않는다.

⚠️ **`knowledge_level`이 null인 경우를 반드시 다룰 것** — θ를 아직 모르는 개념
(배치 중간·관측 부족)이다. `level_label`은 n=0에서도 라벨을 주므로 종전 화면은
언제나 무언가를 보였다. **null에서 빈칸이 되면 회귀다.**

⚠️ **`level_label` 축을 지우지 말 것.** 서버 스키마가 *"두 축이 공존한다"*고
의도로 적어 두었다(`schemas/progress.py:186`). 바꾸는 것은 **화면 표기**뿐이다.

---

⚠️ **⑫가 동결 전 유일한 실질 리스크다.** 사용자가 직접 지시한 결함이고 아직
미착수다. B팀이 셋을 물었으므로 **순서가 곧 우선순위다** — 라벨은 「조금씩만」이고
난이도 표기는 반나절이지만, **⑫는 기능이 아예 없다.**

---

## §2 ✂️ 자른 것 — 사유

**전부 이월이다. 「안 한다」가 아니라 「이 롤링에 안 넣는다」.**

| 항목 | 사유 |
|---|---|
| ~~⑬ 탐구 조작 변수~~ | 🔴 **되살렸다 → §1 A팀.** PM이 「팀당 1건」이라는 **스스로 만든 제약**으로 잘랐고 클라이언트가 정정했다(*"왜 너가 판단해서 잘라"*). **배점 근거가 가장 강한 항목이었다** — 그런 것을 PM 판단으로 떨어뜨리는 것은 가지치기가 아니다 |
| ~~난이도 표기 잔존 2화면~~ | 🔴 **되살렸다 → §1 B팀**(클라이언트 직접 지시) |
| **ⓐ 컨테이너 오버플로 잘림** | `.wm-track` 아래 끝 노드 밑변 11.25~17.25px(폭 820/900/1024). ⚠️ **근거가 양쪽 다 기하값뿐이다** — A조도 PM도 잘린 그림을 눈으로 못 봤다. 아이콘 잘림은 ⑪(#145)로 이미 완화됐으므로 **불확실한 것을 하루 남기고 넣지 않는다** |
| **경계층 수치 모델 보드** | 엔진 확장 필요. 8/20 불가 |
| **API 훈련 고도화** | 비용 게이트 G1·G2 + **키 미포함 제출**과 충돌(심사 중 안 돌아감) |
| **과학적 정합성·자료 검증** | 사용자 지시로 배제(QA 항목) |
| **문항 난이도 순차·체감 구성** | 🔴 **자른 것이 아니라 8/21로 옮겼다**(클라이언트: *"이것은 QA에 가까워서 21일 진행"*). ⚠️ **동결이 8/21 18:00(배포 기준)이므로 그날 창이 좁다** — 배포까지 가야 반영이다. 착수 시 담당·범위를 §1에 추가한다. #136(최상위부터 시작)·#138(인정/푼 것)은 **시작 위치와 진행 표시**를 고친 것이고 문항 자체의 난이도 계단은 미착수다 |
| ~~대상별 교과목 구분~~ | 🔴 **PM 오류 — 자를 항목이 아니었다.** 실측: `courses.json` **2건**(기초과학 · 날씨와 기후) + `level_group` 4단계(elementary·middle_high·adult·expert) + 교과 단계 10종. **이미 갈려 있다.** 「태그×단계 72칸 중 31칸」은 **「유닛의 단계 층화」**(같은 유닛을 여러 단계로 내는 것)의 근거이고 **교과목 구분과 다른 항목**인데, 이름이 비슷해 잘못 붙였다 |
| **FU-N 개칭 · §5.25** | 문서. 열린 PR이 병합된 뒤 **한 번에** 해야 한다(15행이 미병합 PR 4개에 흩어짐) |
| **선지 길이 편향** | 이월 유지 |

---

## §3 ✅ 이미 있는 것 — **제출물로 넘긴다**

사용자 목록 20건 중 **7건이 이미 구현돼 있었다.** 심사위원이 못 본 기능은 0점이다.
**8/20 제출물(시연 영상·README) 작업의 입력이 이 표다.**

| 항목 | 자리 |
|---|---|
| 위성지도 | `explore/SatelliteView.jsx` + `satelliteField.js` |
| 시뮬레이션 3D | 단면 **GL 20종 + SVG 폴백 20종** |
| 일기도 보드 | **#131** — 일기도 2판 + 대기역학 |
| 탐구 활동(태풍·기후) | `TyphoonSimPage`(변수 3) · `ClimateSimPage` · `SandboxPage` |
| 교과 난이도 표기 | 10단계 「초등 3~4학년」…「기상청 현업」 + 부제 (보드는 #32b 전환 완료) |
| 초등 톤 | **#133** |
| 순차 난이도 | **#136**(최상위부터 시작) · **#138**(인정/푼 것 분리) |

---

## §4 롤링 화물 — 열린 PR 16건

**결함 수리 13건**
#134 진도 불러오기·닉네임·수준·목표(①②③④⑤) · #135 목표 9↔10(⑥) ·
#138 완료 게이지(⑦) · #136 시작 위치(⑧) · #137 보드 잠금(⑨) ·
**#143** 다음단계 잠금(⑩) · **#145** 아이콘(⑪ + ⓑ) · #132 태풍 백지

**모식도** **#144** 홍수(4차) · **#146** 산불(불티 역풍 = **사실 오류**) · #142 MT-22 · #118

**콘텐츠·문서** #131 보드 2판 · #133 초등 톤 · #130 · #141

⚠️ **코드 PR에 조율 문서가 섞인 자리 2곳** — 판정자가 미리 알아야 한다:
· **#143**에 `ROLLING_0820_SCOPE.md`(이 문서) · **#145**에 §5.25 대장

---

## §5 PM 역할 전환 (클라이언트 지시)

*"너는 가지를 쳐야지 이제"*

⇒ **PM은 구현을 멈춘다.** 이 시점부터 PM이 하는 것은:
1. **가지치기** — 신규 요구가 오면 §1·§2에 넣고 팀에 배정하거나 자른다
2. **게이트 준비** — 화물 목록·근거·미확인 항목을 판정 가능한 형태로
3. **조율** — 소유 충돌·기점 지정·중복 방지

**PM이 직접 짠 것은 여기서 끝난다**: ⑥(#135) · ⑩(#143) · 홍수 4차(#144) ·
낡은 수 정리 · 8/19 문서 구조. **이후 코드는 두 팀이 짠다.**

⚠️ **이유가 있다.** PM이 구현에 들어가 있는 동안 놓친 것이 실제로 있었다 —
확인 안 한 것을 근거로 지시해 A조가 **세 번** 잡았고(재개 목록·홍수 지시 방향·
탐구 변수 계수), 홍수 커밋에서 약속한 편집 하나를 빠뜨렸다.

---

## §6 🔴 확정 시점에 남은 미확인 — 숨기지 않는다

| 무엇 | 상태 |
|---|---|
| **실브라우저 렌더** | 🔴 홍수·MT-22·산불의 「이렇게 보인다」를 **아무도 확정 못 했다.** PM은 브라우저 확장이 안 붙고, B조는 갤러리에서 봤으나 **판형 260×150이라 수면선·창문선을 확정 못 했다.** 최종 판정은 **사용자가 갤러리에서** 한다 (`localhost:5311/gallery.html`) |
| **ⓐ 오버플로 잘림** | 기하값만 있고 **잘린 그림을 아무도 못 봤다**(§2에서 자른 이유) |
| **`stash@{0}`** | 손대지 않았다. 내용은 `ROLLING_0819.md`로 구조했고 stash 자체는 그대로 남아 있다 |
| **주최측 문의서** | `docs/team/INQUIRY_LOGIN_DEPLOY_0819.md` — **미발송.** 팀명 미기재 |
